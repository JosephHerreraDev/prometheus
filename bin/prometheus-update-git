#!/bin/bash

set -e

echo -e "\e[32mUpdate Prometheus\e[0m"

prometheus-update-time

git -C $PROMETHEUS_PATH pull --autostash
git -C $PROMETHEUS_PATH --no-pager diff --check || git -C $PROMETHEUS_PATH reset --merge
